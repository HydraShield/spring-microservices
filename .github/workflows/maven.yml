# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: |
        cd ./service-1
        mvn clean install

        cd ../service-2
        mvn clean install

        cd ../service-3
        mvn clean install

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Docker
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker images
        run: |
          ls service-1/
          ls service-1/target/
          docker build -t hydrashield/service-1:latest -f service-1/Dockerfile .
          docker push hydrashield/service-1:latest

          docker build -t hydrashield/service-2:latest -f service-2/Dockerfile .
          docker push hydrashield/service-2:latest

          docker build -t hydrashield/service-3:latest -f service-2/Dockerfile .
          docker push hydrashield/service-3:latest
